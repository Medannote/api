# API Configuration
# Copy this file to .env and adjust values as needed

# CORS Configuration - comma-separated list of allowed origins
# Use "*" for development only, specify exact origins in production
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# API Base URL for batch processing (internal API calls)
# This should be the URL where your API is accessible
API_BASE_URL=http://localhost:8000

# Rate Limiting Configuration
# Number of requests allowed per period
RATE_LIMIT_CALLS=100
# Time period in seconds
RATE_LIMIT_PERIOD=60

# Server Configuration (optional, can be set via uvicorn command)
# HOST=0.0.0.0
# PORT=8000
